# 安全与隐私报告

## 不要公开提交敏感信息

请不要在 Issue、Pull request、日志、样例数据或提交历史中提交：

- API key、访问令牌、Cookie、密码或私钥；
- 姓名、电话、邮箱、账号标识等不必要个人信息；
- 未获授权的原始职位数据或受许可限制的数据。

如果误提交了凭证，应立即在对应服务中撤销或轮换，并通过私下渠道通知维护者。仅删除文件并不能清除 Git 历史中的凭证。

## 报告漏洞

请使用 GitHub 仓库提供的私下漏洞报告入口（如果仓库已启用 Security Advisories）。在入口不可用时，请通过维护者的 GitHub 个人页面私下联系，不要在公开 Issue 中披露可利用细节。

报告应包含：

1. 受影响的版本或提交；
2. 复现条件和最小复现步骤；
3. 可能的影响范围；
4. 建议的修复方向（如有）。

## 设计边界

项目默认离线处理职位级数据。数据源适配器必须遵守来源许可和访问规则，不实现绕过登录、验证码、访问控制或频率限制的功能。
